{
  "name": "effortd",
  "version": "0.1.0",
  "description": "Reasoning-spend policy gateway for AI coding agents — normalize, observe, and govern model effort across providers",
  "license": "MIT",
  "author": "Vivek Vinodh",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "effortd": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run test",
    "prepublishOnly": "npm run verify && npm run build"
  },
  "dependencies": {
    "yaml": "^2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3"
  }
}
